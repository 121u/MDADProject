<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mdadproject">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.hardware.camera" />
    <uses-permission android:name="android.hardware.camera.autofocus" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ZxingScannerActivity"></activity>
        <activity android:name=".StaffQrScanner" />
        <activity android:name=".UserQrCode" />
        <activity
            android:name=".Playground"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".UserPetDetails" />
        <activity
            android:name=".UserPets"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".UserProfile"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".UserBookAppointment"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".RegisterPetDetails" />
        <activity android:name=".RegisterChoosePet" />
        <activity android:name=".RegisterUserPass" />
        <activity
            android:name=".RegisterDetails"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>